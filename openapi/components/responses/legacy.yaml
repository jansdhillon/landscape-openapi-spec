# SPDX-License-Identifier: Apache-2.0

LegacyActionResponse:
  description: Result returned by a legacy action.
  content:
    application/json:
      schema:
        $ref: "../schemas/legacy.yaml#/LegacyActionResult"
