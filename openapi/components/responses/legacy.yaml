# SPDX-License-Identifier: Apache-2.0

LegacyActionResponse:
  description: Result returned by a legacy action.
  content:
    application/json:
      schema:
        type: object
        description: "The response body varies by action."
